import React from 'react';
import ReactDOM from 'react-dom/client';

// The portfolio is already fully implemented in index.html
// This file serves as the entry point for any additional React components
// if needed in the future.

// For now, the portfolio works with vanilla JavaScript in the HTML file
// which includes all animations, scrolling effects, and interactivity

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <div />
  </React.StrictMode>,
);
